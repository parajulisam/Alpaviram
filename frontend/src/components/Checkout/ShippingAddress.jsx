import React from "react";
import { useForm } from "react-hook-form";
import { useNavigate } from "react-router-dom";

const ShippingAddress = ({ register, reset }) => {
  return <div></div>;
};

export default ShippingAddress;
